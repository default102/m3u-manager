version: '3.8'

services:
  m3u-manager:
    build: .
    container_name: m3u-manager
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/db.sqlite
    restart: always
